// This import adds custom matchers like toBeInTheDocument(),
// toHaveTextContent(), toBeVisible() etc. to Jest's expect().
// Without this, those matchers would be undefined.
import '@testing-library/jest-dom';
